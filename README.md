# Recreatathon

Tiny **Express** API + **static** UI for Git practice.

```bash
npm install && npm start
```

Open http://127.0.0.1:3000 — the page loads JSON from `/api/meta`.

- `public/` — HTML/CSS (and inline JS)
- `server/index.js` — server
